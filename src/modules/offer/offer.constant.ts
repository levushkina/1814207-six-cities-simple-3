export const DEFAULT_OFFER_COUNT = 60;

export const DEFAULT_NEARBY_OFFER_COUNT = 3;

export enum TitleRange {
  Min = 10,
  Max = 100,
}

export enum DescriptionRange {
  Min = 20,
  Max = 1024,
}

export enum ImageRange {
  Min = 6,
  Max = 6,
}
